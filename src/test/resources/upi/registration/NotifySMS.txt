{
       "deviceIds": [
           "${deviceId}"
       ],
       "mobile": "${mobile}",
       "simIds": [
           "${simId}"
       ],
       "status": "${status}",
       "upiRegistrationId": ${upiRegistrationId}
}