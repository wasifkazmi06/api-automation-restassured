{
    "userDetails": {
        "mobile": "${mobile}",
        "email": "${email}"
    },
    "amount": {
        "value": "${amount}",
        "currency": "${currency}"
    },
    "address": {
        "street1": "${street1}",
        "street2": "${street2}",
        "city": "${city}",
        "state": "${state}",
        "country": "${country}",
        "zip": "${zip}",
        "landmark": "${landmark}",
        "residenceType": "${residenceType}"
    },
    "customParams": {
    }
}